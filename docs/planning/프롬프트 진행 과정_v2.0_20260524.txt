git clone -b refactor/fullstack/v2.0_20260521 https://github.com/Myung-Il/ELAW.git

/init

웹에서 실행할 수 있게 해줘.

When it completes, I'll:
  1. Check the final counts (JobProblem, JobProblemCluster, ProblemEdge,
  LearningPathMeta).(최종 데이터 개수 확인 (JobProblem, JobProblemCluster, ProblemEdge, LearningPathMeta))
  2. Verify the quiz pipeline API endpoints with actual data.(실제 데이터를 사용해 퀴즈 파이프라인 API 엔드포인트 검증)
  3. Test the ML recommendation flow end-to-end.(ML 추천 흐름 엔드투엔드(E2E) 테스트)


./DB/JobProblems 폴더에서 가져와 저장한 데이터를 이용하여 사용자의 설정된 직무별로 공부목록 문제가 보여지도록 설정해줘


현재 상태를 백업해줘.

/review

----------
성능 최적화를 진행해줘.

이 프로젝트를 github에서 첫 클론을 받아서 실제 웹상에서 볼 수 있는 실행 단계를 ./docs/ExecutionSteps.md 파일로 저장해줘.
