##진행할 것##
Update ELAW ~~~ 세션
이전 작업인 '◻ 실계정 4개 ETL 동기화 + 가상 플랫폼 데이터 정리'을 이어서 진행해 주고, 실계정 4개에 대한 정보야 "./DB/실사용자 4명(우리팀 정보).txt"

##진행 중##
새로운 세션
풀백 서버를 도커 컨테이너 환경에 구축하려고 해.
이 상황에 대한 PRD를 만들어서 단계별로 진행하려고 하는데, 물어볼 것 있으면 물어봐 줘.

· 풀백 서버의 목적이 무엇인가요? → 주 서버를 로컬 PC로설정하고 풀백 서버를  도커 컨테이너 환경에 설정하여, 도커 컨테이너 서버의 다운 현상을 방지하고 싶어.
     · 풀백 서버(Docker 컨테이너)를 어디에서 돌리나요? → 학교/팀 내
     다른 PC·서버
     · AI 포트폴리오 생성(Ollama mybot)은 풀백 서버에서 어떻게
     처리할까요? Ollama는 CPU 추론 2~4분짜리 무거운 의존성이라
     컨테이너에 넣을지가 핵심 분기점입니다. → Ollama 컨테이너 포함
     · 주 서버 → 풀백 서버 전환은 어떻게 이루어지길 원하나요?
     (프론트는 Vercel의 NEXT_PUBLIC_API_URL이 백엔드 주소를
     결정합니다) → 수동 전환 스크립트

1. 서버 주소: 172.26.21.246(학교 내부)/220.67.89.246(학교 외부)
2. SSH 계정명: root (포트: 12278)
3. 인증 방식: 비밀번호
4. sudo 사용 가능 여부: 불가능 할 것 같아. NAS 서버를 사용하고 NAS/volume 디렉토리에서 작업을 해

type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh -p 12278 root@220.67.89.246 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys && chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys"